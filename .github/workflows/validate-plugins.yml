name: validate-plugins.yml
on:
  pull_request:
  push:
    branches: [master]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate marketplace.json
        run: |
          python -c "import json; json.load(open('.claude-plugin/marketplace.json'))"
